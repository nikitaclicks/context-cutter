# context-cutter-mcp (npm launcher)

`npx context-cutter-mcp` downloads and runs the matching native ContextCutter MCP binary from GitHub Releases.

## Usage

```bash
npx -y context-cutter-mcp
```

Use in MCP configs as:

```json
{
  "command": "npx",
  "args": ["-y", "context-cutter-mcp"]
}
```
