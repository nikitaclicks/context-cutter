name: Tests

on:
  pull_request:
  push:
    branches: ["main"]

concurrency:
  group: tests-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Python tooling
        run: python -m pip install ruff

      - name: Rust fmt
        run: cargo fmt --check

      - name: Rust clippy
        run: cargo clippy -- -D warnings

      - name: Ruff
        run: ruff check python/ tests/

  test-matrix:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install maturin
        run: python -m pip install maturin

      - name: Build and install Rust extension
        run: |
          maturin build --features python --out dist
          pip install --find-links dist context-cutter

      - name: Install dev dependencies
        run: python -m pip install -e ".[dev]"

      - name: Run Rust tests
        run: cargo test

      - name: Run Python tests
        run: pytest -m "not ai_e2e_offline and not ai_e2e_live"

  coverage:
    continue-on-error: true
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-llvm-cov
        run: cargo install cargo-llvm-cov --locked

      - name: Install maturin
        run: python -m pip install maturin

      - name: Build and install Rust extension
        run: |
          maturin build --features python --out dist
          pip install --find-links dist context-cutter

      - name: Install project + coverage deps
        run: |
          python -m pip install -e ".[dev]"
          python -m pip install pytest-cov

      - name: Rust coverage
        run: cargo llvm-cov --workspace --lcov --output-path rust.lcov

      - name: Python coverage
        run: pytest --cov=python/context_cutter --cov-report=xml

      - name: Upload to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./rust.lcov,./coverage.xml
          fail_ci_if_error: false
